entity: Promotion
items:
  promotionOne{1..100}:
    description: '{{lorem.sentence}}'
    beginDate: '{{date.past}}'
    endDate: '{{date.future}}'
    product: '@productOne($current)'
    user: '@user($current)'
    brand: '@brand($current)'
  promotionTwo{1..100}:
    description: '{{lorem.sentence}}'
    beginDate: '{{date.past}}'
    endDate: '{{date.future}}'
    product: '@productTwo($current)'
    user: '@user($current)'
    brand: '@brand($current)'
  promotionThree{1..3}:
    description: '{{lorem.sentence}}'
    beginDate: '{{date.past}}'
    endDate: '{{date.future}}'
    product: '@productOne($current)'
    user: '@user($current)'
    store: '@store($current)'
