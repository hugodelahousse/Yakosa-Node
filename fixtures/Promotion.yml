entity: Promotion
items:
  promotionOne{1..30}:
    description: '{{lorem.sentence}}'
    price: 100
    promotion: 50
    type: 1
    beginDate: '{{date.past}}'
    endDate: '{{date.future}}'
    product: '@productOne($current)'
    user: '@user($current)'
    brand: '@brand($current)'
  promotionTwo{1..30}:
    description: '{{lorem.sentence}}'
    price: 100
    promotion: 50
    type: 2
    beginDate: '{{date.past}}'
    endDate: '{{date.future}}'
    product: '@productTwo($current)'
    user: '@user($current)'
    brand: '@brand($current)'
  promotionThree{1..3}:
    description: '{{lorem.sentence}}'
    price: 100
    promotion: 50
    type: 0
    beginDate: '{{date.past}}'
    endDate: '{{date.future}}'
    product: '@productOne($current)'
    user: '@user($current)'
    store: '@store($current)'
