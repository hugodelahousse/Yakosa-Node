entity: ListProduct
items:
  listProductOne{1..50}:
    quantity: '{{random.number}}'
    list: '@shoppingList($current)'
    product: '@productOne($current)'

  listProductTwo{1..50}:
    quantity: '{{random.number}}'
    list: '@shoppingList($current)'
    product: '@productTwo($current)'

  listProductThree{1..50}:
    quantity: '{{random.number}}'
    list: '@shoppingList($current)'
    product: '@productThree($current)'
